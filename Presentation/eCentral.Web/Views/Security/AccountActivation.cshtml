@model AccountActivationModel
@{
    Layout = "~/Views/Shared/_Container.cshtml"; //default layout
    Html.AddTitleParts("Account Activation");
}
<div class="loginContainer">
    <div class="form-row row-fluid">
        <div class="span12">
            <div class="row-fluid">
                <div class="page-header">
                    <h4 class="blue">
                        Welcome @Model.ContactName</h4>
                </div>
            </div>
        </div>
    </div>    
    <div class="form-row">
        @Html.Partial("_Notifications")
    </div>
    @using (Html.BeginForm(null, null, FormMethod.Post, new { @id = "loginForm", @name = "loginForm", @class = "form-horizontal" }))
    {
        <div class="form-row row-fluid">
            <div class="span12">
                <div class="row-fluid">
                    @Html.LabelFor(m => m.UserName, new { @class = "span12" }, "<span class=\"icon16 icomoon-icon-user right gray marginR10\"></span>")
                    @Html.TextBoxFor(m => m.UserName, new { @class = "span12", tabindex = 1, @readonly = "readonly" })
                </div>
            </div>
        </div>
        <div class="form-row row-fluid">
            <div class="span12">
                <div class="row-fluid">
                    @Html.LabelFor(m => m.Password, new { @class = "span12" }, "<span class=\"icon16 icomoon-icon-lock right gray marginR10\"></span>")
                    @Html.PasswordFor(m => m.Password, new { @class = "span12", tabindex = 2, placeholder = "***********" })
                    @Html.ValidationMessageFor(m => m.Password)
                </div>
            </div>
        </div>
        <div class="form-row row-fluid">
            <div class="span12">
                <div class="row-fluid">
                    @Html.LabelFor(m => m.ConfirmPassword, new { @class = "span12" }, "<span class=\"icon16 icomoon-icon-lock right gray marginR10\"></span>")
                    @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "span12", tabindex = 2, placeholder = "***********" })
                    @Html.ValidationMessageFor(m => m.ConfirmPassword)
                </div>
            </div>
        </div>
        <div class="form-row row-fluid">
            <div class="span12">
                <div class="row-fluid">
                    <div class="form-actions">
                        <div class="span12 controls">
                            @Html.HiddenFor(m => m.Token)
                            @Html.HiddenFor(m => m.UserId)
                            <button type="submit" class="btn btn-info right" id="loginBtn">
                                <span class="icon16 icomoon-icon-key-2 white"></span>Activate</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    @Html.Partial("_Copyright")
</div>
@section inlineScripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            $("body").addClass("loginPage");
            $("input, textarea, select").not('.nostyle').uniform();
        });
    </script>
}
